{
  "name": "jonjibot",
  "version": "0.0.2",
  "description": "A collection of various web tools made by *world famous* twitch streamer jonji13 in his spare time.",
  "main": "dist/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build && copyfiles -u 1 ./src/**/*.html ./dist && copyfiles -u 1 ./src/**/*.md ./dist",
    "clean": "tsc --build --clean",
    "serve": "npx http-server ./dist",
    "buildserve": "npm run build && npm run serve"
  },
  "author": "Jon Ginty",
  "license": "MIT",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "http-server": "^13.0.2",
    "typescript": "^4.4.3"
  }
}
